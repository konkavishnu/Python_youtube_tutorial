from math import pi   # --> importing the function inside the module
import random as rdm  # --> using alias
from enum import Enum

#  NOTE :

print(pi)

vishnu = rdm.choice

print(__name__)

print(dir(rdm))  # --> dir is used to view the functions of the modules
